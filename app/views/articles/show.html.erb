<h1>Show Article</h3>

<p>
	<strong>Title:</strong>
	<%= @article.title %>
</p>

<p>
	<strong>Text:</strong>
	<%= @article.text %>
</p>

<%= link_to 'Edit Article', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>

<hr/>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<hr/>

<h2>Comments</h2>
<p>Note: the bottom-most comment is buggy. It should not be there in the first place. Destroy button for that buggy comment would not work. Need fixing. <p>
<%= render @article.comments %>
